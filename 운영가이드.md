# 더베이직플랜 사이트 운영 가이드

`fire-your-seo-agency` 스킬의 5개 레인(SEO·AEO·GEO·LLMO·NEO) 기준으로 만든 정적 사이트입니다.

## 새 글 발행하는 법 (가장 자주 하는 일)

`src/content/posts/` 에 `.md` 파일 하나만 추가하면 자동으로 페이지가 됩니다.
네이버 원고를 붙여넣고 맨 위 frontmatter만 채우세요.

```markdown
---
title: 사람들이 검색창에 치는 질문 그대로
description: 150~160자, 클릭할 이유가 있는 한 문장
answer: 첫 화면 직답. 40자 내외 결론 먼저. (AEO 인용 포인트)
pubDate: 2026-08-28
keywords: [키워드1, 키워드2]
faq:
  - q: 실제 검색 질문
    a: 데이터로 확정되는 답만
---

본문. h2/h3 소제목, 표(근거 수치), 결론 먼저 원칙.
```

- **질문 하나 = 글 하나.** 한 글에 여러 주제를 담지 마세요.
- `faq`를 채우면 FAQPage 구조화 데이터가 자동 생성됩니다.
- 발행 후 실제로 그 질문을 네이버·구글에 검색해 인용 여부를 기록하세요.

## 네이버 블로그 → GEO 글 자동 임포트 (핵심 기능)

네이버 블로그 글을 가져와 GEO/AEO 구조로 재작성해 발행하는 파이프라인입니다.
`homho-main` 참고구현을 정적 사이트용으로 포팅했습니다.

```
네이버 URL → [scrape] → .staging/*.md → [리라이팅] → src/content/posts/*.md → 배포
```

### 1단계: 글 목록 보기
```bash
npm run naver:list -- <블로그주소 또는 blogId>      # 예: npm run naver:list -- 내블로그아이디
```

### 2단계: 글 가져오기 (scrape, API 키 불필요)
```bash
npm run naver:scrape -- "<글 URL>"                   # .staging/ 에 깨끗한 마크다운 저장
```
네이버 iframe·비표준 JSON·이미지 제거까지 자동 처리합니다. 이미지는 빠지므로 필요하면 직접 넣으세요.
⚠️ 저작권: **자기 소유(또는 대행 계약된) 블로그 글에만** 사용하세요.

### 3단계: GEO로 리라이팅해 발행 — 두 가지 경로

**(A) Claude에게 맡기기 — 기본, 추천 (키/비용 없음, 품질 최고)**
Claude Code 대화창에:
> `.staging/파일이름.md 를 GEO 칼럼으로 리라이팅해서 src/content/posts 에 발행해줘`

Claude가 AEO/GEO 지침(BLUF·통계/인용/출처·질문형 헤딩·FAQ)에 맞춰 재작성하고
frontmatter까지 채워 `src/content/posts/`에 글을 만듭니다.

**(B) 자동(무인 배치) — API 키 있을 때**
```bash
GEMINI_API_KEY=... npm run naver:rewrite -- .staging/파일이름.md      # Gemini Flash 권장
# 또는
OPENAI_API_KEY=... npm run naver:rewrite -- .staging/파일이름.md --model gpt-4o
```
결과는 `draft: true` 초안으로 저장됩니다. 검수 후 frontmatter의 `draft`를 지우거나 `false`로 바꾸면 게시됩니다.

## 로컬 미리보기 / 빌드

```bash
npm run dev       # http://localhost:4321 실시간 미리보기
npm run build     # dist/ 에 정적 HTML 생성
npm run preview   # 빌드 결과 미리보기
```

## 배포 전 반드시 바꿀 것 (2곳)

1. **문의폼 엔드포인트** — `src/pages/index.astro` 의 `FORM_ENDPOINT`
   - [formspree.io](https://formspree.io) 무료 가입 → 폼 생성 → 받은 주소(`https://formspree.io/f/xxxx`)로 교체
   - 수신 메일: ultravapor@naver.com
2. **도메인** — 도메인을 사면 `astro.config.mjs` 의 `site` 값과
   `public/robots.txt`·`public/llms.txt` 안의 URL을 새 도메인으로 교체

## 배포 (무료)

1. 이 폴더를 GitHub 저장소로 올림
2. [vercel.com](https://vercel.com) 가입 → Import → 이 저장소 선택 → 프레임워크 Astro 자동 인식 → Deploy
3. `the-basic-plan.vercel.app` 주소로 라이브

## 배포 후 (측정 기반 세팅)

- [ ] 구글 서치콘솔 등록 + 사이트맵(`/sitemap-index.xml`) 제출 + 색인 요청
- [ ] 빙 웹마스터도구 등록 (GSC에서 원클릭 임포트) — ChatGPT 검색이 빙 색인 사용
- [ ] 네이버 서치어드바이저 등록 + 사이트맵 제출 (Yeti 허용 확인)
- [ ] 기준선 기록: 노출·클릭·AI 인용(질문 5~10개 O/X) → 14일 후 재측정
```
```
